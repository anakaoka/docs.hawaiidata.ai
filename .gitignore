# Server snapshots and operational docs - never commit to this public repo
servers/
server-snapshot-*.tar.gz
server-snapshot-*.tgz
server-snapshot-*/

# Anything explicitly marked as containing secrets
**/secret-*
**/*.private
**/*.pem
**/*.key
!**/cert-inventory.txt

# Local-only review notes
SCRATCH.md
.scratch/
